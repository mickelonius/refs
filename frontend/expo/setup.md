### create project
```bash
# have to make sure expo gets installed 1st
nvm list available

nvm install 20.12.0
nvm use 20.12.0
node --version
npm --version

npx expo
 
yarn create expo-app test-expo-app
npx create-expo-app
cd test-expo-app

npx expo install react-native-web react-dom @expo/metro-runtime
npm run web


```

